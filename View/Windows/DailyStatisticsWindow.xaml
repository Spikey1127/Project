<Window x:Class="CalorieCalendarProg.View.Windows.DailyStatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:vm="clr-namespace:CalorieCalendarProg.ViewModel"
        Title="Napi statisztika" Height="350" Width="500"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="StatsText" Margin="0,0,0,10"/>
        <lvc:CartesianChart Series="{Binding SeriesCollection}" Margin="10">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Típus" Labels="{Binding Labels}" />
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Kalória (kcal)" />
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>

        <CheckBox Content="Dark Mode" Checked="Dark_Checked" Unchecked="Dark_Unchecked"/>

    </Grid>
</Window>
